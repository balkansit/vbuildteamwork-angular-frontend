import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.balkansit.boutique',
  appName: 'boutique',
  webDir: 'deploy-boutique-frontend',
};

export default config;
